import React from 'react';
import {Alert, Button, Text, View} from 'react-native';

const Home = ({navigation}: any) => {
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

export default Home;
